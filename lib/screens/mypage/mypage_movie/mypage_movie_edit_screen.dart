import 'package:flutter/material.dart';

class MyPageMovieEditScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
